<!-- 
 <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Manage Products</title>

  <!-- ✅ Load jQuery first -->
  <!-- <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script> -->

  <!-- ✅ Load Bootstrap -->
  <!-- <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script> -->

    <!-- ✅ Custom script -->
    <!-- <script>
        const productModal = $('#productModal'); -->

        // $(function () {
        //     // Load product list
        //     $.get(productListApiUrl, function (response) {
        //         if (response && Array.isArray(response)) {
        //             let table = '';
        //             $.each(response, function (index, product) {
        //                 table += `
        //                     <tr data-id="${product.product_id}" 
        //                         data-name="${product.name}" 
        //                         data-uom="${product.uom_name}" 
        //                         data-price_per_unit="${product.price_per_unit}">
        //                         <td>${product.name}</td>
        //                         <td>${product.uom_name}</td>
        //                         <td>${product.price_per_unit}</td>
        //                         <td>
        //                             <span class="btn btn-xs btn-danger delete-product">Delete</span>
        //                             <span class="btn btn-xs btn-primary edit-product">Edit</span>
        //                         </td>
        //                     </tr>`;
        //             });
        //             $("table").find('tbody').html(table);
        //         }
        //     });

            // Edit product
            // $(document).on("click", ".edit-product", function () {
            //     const tr = $(this).closest('tr');
            //     $("#id").val(tr.data('id'));
            //     $("#name").val(tr.data('name'));
            //     $("#uom").val(tr.data('uom'));
            //     $("#price_per_unit").val(tr.data('price_per_unit'));
            //     productModal.find('.modal-title').text('Edit Product');
            //     productModal.modal('show');
            // });

            // Delete product
            // $(document).on("click", ".delete-product", function () {
            //     const tr = $(this).closest('tr');
            //     const productId = tr.data('id');

            //     if (confirm("Are you sure you want to delete this product?")) {
            //         $.ajax({
            //             url: `${deleteProductApiUrl}/${productId}`,
            //             method: 'DELETE',
            //             success: function () {
            //                 tr.remove();
            //             },
            //             error: function () {
            //                 alert("Failed to delete product.");
            //             }
            //         });
            //     }
            // });

            // Save product
    //         $("#saveProduct").click(function () {
    //             const name = $("#name").val().trim();
    //             const uom = $("#uom").val().trim();
    //             const price = $("#price_per_unit").val().trim();

    //             if (!name || !uom || !price || isNaN(price)) {
    //                 alert("Please fill out all fields correctly.");
    //                 return;
    //             }

    //             const data = $("#productForm").serializeArray();
    //             const requestPayload = {};
    //             $.each(data, function (i, field) {
    //                 requestPayload[field.name] = field.value;
    //             });

    //             $.ajax({
    //                 url: saveProductApiUrl,
    //                 method: requestPayload.id ? 'PUT' : 'POST',
    //                 data: requestPayload,
    //                 success: function () {
    //                     productModal.modal('hide');
    //                     location.reload();
    //                 },
    //                 error: function () {
    //                     alert("Error saving product.");
    //                 }
    //             });
    //         });
    //     });
    // </script>

-->